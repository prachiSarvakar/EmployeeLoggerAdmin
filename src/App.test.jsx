import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

describe("Render app component", () => {
  const Wrapper = (props) => <App {...props} />;
  test("Render app component", () => {});
});
